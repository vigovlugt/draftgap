import { Component } from "solid-js";
import { Panel, PanelHeader } from "../../common/Panel";

export const ItemSetStats: Component = () => {
    return (
        <Panel>
            <PanelHeader>Builds</PanelHeader>
        </Panel>
    );
};
