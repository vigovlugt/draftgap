import { Component } from "solid-js";
import { Panel, PanelHeader } from "../../common/Panel";

export const RecommenedBuild: Component = (props) => {
    return (
        <Panel>
            <PanelHeader>Recommended Build</PanelHeader>
        </Panel>
    );
};
