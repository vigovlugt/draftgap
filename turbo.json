{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "typecheck": {},
        "build": {
            "dependsOn": ["typecheck"],
            "outputs": ["dist/**"]
        },
        "lint": {}
    }
}
